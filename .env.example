# Vercel Postgres Connection
POSTGRES_URL=postgresql://user:password@host:5432/database?sslmode=require

# OpenAI API Key (fallback if student doesn't provide one)
OPENAI_API_KEY=sk-...

# Port (Railway will set this automatically)
PORT=3000
